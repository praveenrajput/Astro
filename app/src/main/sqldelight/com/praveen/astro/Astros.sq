CREATE TABLE People (
  name TEXT PRIMARY KEY,
  craft TEXT NOT NULL
);

insert:
INSERT INTO People(name, craft)
VALUES ( ?, ?);

insertAstro:
INSERT INTO People(name, craft)
VALUES ?;

deleteAll:
DELETE FROM People;

selectAstros:
SELECT *
FROM People;
